{
  "name": "GuestDB",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "GuestDB",
      "onAutoForward": "openPreview",
      "elevateIfNeeded": true
    }
  },
  "containerEnv": {
    "DJANGO_SUPERUSER_USERNAME": "admin",
    "DJANGO_SUPERUSER_EMAIL": "admin@example.com",
    "DJANGO_SUPERUSER_PASSWORD": "admin12345"
  },
  "postCreateCommand": "pip install --no-cache-dir -r requirements.txt && python manage.py migrate && python manage.py setup_roles && python manage.py seed_users && python manage.py createsuperuser --noinput || true && python manage.py seed_mock_contacts --total 1000 || true",
  "customizations": {
    "codespaces": {
      "codespacesAdditionalRepositories": []
    }
  }
}